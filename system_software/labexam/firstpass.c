#include<stdio.h>
#include<stdlib.h>
#include<string.h>


void main(){
    FILE *source=fopen("source.txt","r"),inter=fopen("intermediate.txt","w");
    char inst[10],label[10],operand[10];
    if(f){
        fscanf(source,"%s %s %s",label,inst,operand);
        if
    }
}